
function JakaCyfra() //przypisanie do przycisków "cyferek"

{
    let temp = ""; //tymczasowa zmienna

    for (i=1; i<=10; i++)
   {
       temp = temp + i;
       //var div = document.createElement("div"); //tworzenie kolejnych divów
        // div.innerHTML = i;
       // document.getElementById("results").appendChild(div); //dodanie do diva results
   }

     // document.getElementById("activities").innerText = "wypisano: " + temp;
return temp;
}
